# Base directories for local data storage. Update paths to point to a fast local disk.
DATA_ROOT=./data

# Persistent ChromaDB storage root. Stored under DATA_ROOT by default.
CHROMA_DB_DIR=./data/chromadb

# SQLite database URL for ingestion metadata and analytics metrics.
SQLITE_URL=sqlite:///data/metadata.db

# Default SentenceTransformers model used for embedding generation.
SENTENCE_TRANSFORMER_MODEL=all-MiniLM-L6-v2
