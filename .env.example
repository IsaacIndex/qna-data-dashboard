# Base directories for local data storage. Update paths to point to a fast local disk.
DATA_ROOT=./data

# Persistent ChromaDB storage root. Uses DATA_ROOT/embeddings by default.
CHROMA_PERSIST_DIR=./data/embeddings
# Legacy override retained for backward compatibility.
# CHROMA_DB_DIR=./data/chromadb

# SQLite database URL for ingestion metadata and analytics metrics.
SQLITE_URL=sqlite:///data/metadata.db

# Default embedding model used for generation.
EMBEDDING_MODEL_ID=nomic-embed-text
# Legacy model env is also supported if set:
# SENTENCE_TRANSFORMER_MODEL=all-MiniLM-L6-v2

# Ingest configuration
INGEST_MAX_BYTES=52428800            # 50 MB per file
INGEST_ALLOWED_TYPES=csv,xls,xlsx,parquet
INGEST_REEMBED_CONCURRENCY=3
INGEST_STORAGE_ROOT=./data/ingest_sources
